# Note: To run this project, you must have the configuration server application running (it must be running on port 8888).
spring:
  application:
    name: ecommerce-v3-dev
  cloud:
    config:
      uri: http://localhost:8888

management:
  endpoints:
    web:
      exposure:
        include: refresh